global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets:
          - "localhost:9090"

  - job_name: "node_exporter"
    static_configs:
      - targets:
          - "{{ web_server_ip }}:9100"
        labels:
          server: "web-server"

      - targets:
          - "{{ app_server_ip }}:9100"
        labels:
          server: "app-server"

      - targets:
          - "{{ db_server_ip }}:9100"
        labels:
          server: "db-server"

      - targets:
          - "{{ monitoring_server_ip }}:9100"
        labels:
          server: "monitoring-server"